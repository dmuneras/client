
import { lessThan } from 'ember-addon-review/helpers/less-than';
import { module, test } from 'qunit';

module('Unit | Helper | less than');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lessThan([42]);
  assert.ok(result);
});

